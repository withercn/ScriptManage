
@{
	ViewBag.Title = "常用脚本管理";
	Layout = "~/Views/Shared/_aside.cshtml";
	ScriptManage.Models.HtmlPager pager = ViewBag.Pager;
}

<div class="module site">
	<header><h3>常用脚本列表</h3></header>
	<div class="contents">
		@using (Html.BeginForm("Del", "Effect"))
		{
			@Html.AntiForgeryToken();
		<table class="tablesorter">
			<thead><tr>
				<th class="header nowrap w10"><span class="all">全选</span> / <span class="none">反选</span></th>
				<th class="header nowrap w20">脚本名称</th>
				<th class="header overflow">脚本描述</th>
				<th class="header w10">启用</th>
				<th class="header w10">操作</th>
			</tr></thead>
			<tbody>
				@foreach (var m in Model)
				{
					<tr>
					<td><input type="checkbox" name="id" value="@m.id" /></td>
					<td>@m.name</td>
					<td>@m.description</td>
					<td>@(m.complete ? "是" : "否")</td>
					<td class="pager">
						<a href="@Url.Action("Del", "Effect", new { id = m.id })">删除</a>
					</td>
					</tr>
				}
			</tbody>
		</table>
			<div class="pager">
				<span class="all">全选</span> / <span class="none">反选</span>
				<a href="javascript:;" class="submit">删除</a>
				@Html.ActionLink("添加", "New", "Effect")
				<a href="@Url.Action("Index", "Effect", new { id = pager.First })">首页</a>
				<a href="@Url.Action("Index", "Effect", new { id = pager.Prev })"><</a>
				@for (var i = pager.Start; i <= pager.End; i++)
				{
					if (i != pager.PageIndex)
					{ 
					<a href="@Url.Action("Index", new { id = i })">@i</a>
					}
					else
					{ 
					<a class="current">@i</a>
				}
				}
				<a href="@Url.Action("Index", "Effect", new { id = pager.Next })">></a>
				<a href="@Url.Action("Index", "Effect", new { id = pager.Last })">末页</a>
				<a>共@(pager.TotalRecord)个特效</a>
			</div>
		}
		<div class="msg">@ViewBag.Message</div>
		<div class="clear"></div>
	</div>
</div>
@section Scripts{
	@Scripts.Render("~/bundles/jquerytable")
}